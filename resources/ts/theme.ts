import { createTheme } from "@mui/material/styles";

export const ZenKurenaidoFont = createTheme({
    typography: {
        fontFamily: ["Zen Kurenaido"].join(","),
    },
});

export const RowdiesFont = createTheme({
    typography: {
        fontFamily: ["Rowdies"].join(","),
    },
});
